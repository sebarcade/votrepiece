import Calculator from './Calculator';

// La section, l'en-tête et l'id="calculateur" sont maintenant gérés directement dans Calculator.tsx
export default function CalculatorSection() {
  return <Calculator />;
}
